<div class="row">
  <div class="well center ">
    <h2>Willkommen zur Fachschaft Informatik</h2>
    <p>Hier könnte könnt ihr in Zukunft Klausuren, Übungsaufgaben und natürlich auch Lösungen herunterladen.</p> 
  </div>
</div>

<% if logged_in?(:user, :admin, :moderator) %>

<div class='row'>
  <% @notifications.each do |notification| %>
      <div class='col-lg-offset-0 col-lg-4 col-md-offeset-0 col-md-6 col-xs-10 col-xs-offset-1'>
        <div class="panel panel-default">
          <img src="<%= create_path(notification.image.url(:card)) %>" class="img-responsive">
          <div class="panel-body">
            <p class='lead'><%= truncate(notification.title, length:50) %></p>
            <p><%= truncate(notification.message, length: 100) %></p>
            <center>
              <p>
               
              </p>
               <div class='panel-footer' style='border-radius: 5px;' >
              <small>
                Created at:  <%= time_ago_in_words(notification.created_at) %> ago,
                <br />
                last updated: <%= time_ago_in_words(notification.updated_at) %> ago
              </small>
               <div/>
            </center>
          </div>
        </div>
    </div>
  <% end %>
</div>

<% else %>


<div class='row'>
  <% @notifications.each do |notification| %>
      <div class='col-lg-offset-0 col-lg-4 col-md-offeset-0 col-md-6 col-xs-10 col-xs-offset-1'>
        <div class="panel panel-default">
          <img src="<%= create_path(notification.image.url(:card)) %>" class="img-responsive">
          <div class="panel-body">
            <p class='lead'><%= truncate(notification.title, length:50) %></p>
            <p><%= truncate(notification.message, length: 100) %></p>
            <center>
              <p>
               
              </p>
               <div class='panel-footer img-circle' >
              <small>
                Created at:  <%= time_ago_in_words(notification.created_at) %> ago,
                <br />
                last updated: <%= time_ago_in_words(notification.updated_at) %> ago
              </small>
               <div/>
            </center>
          </div>
        </div>
    </div>
  <% end %>
</div>


<% end %>



</div>

  
  

